Following tools are required to be installed on windows to perform automated testing for native application:

> Windows Application Driver: For server .
> Windows SDK: For inspecting the page elements using inspect.exe.
> Win App UI Recorder: For getting the xpath of the elements.

